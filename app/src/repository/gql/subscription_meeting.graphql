subscription SubscribeMeeting($id: String!) {
  notification (id: $id) {
    __typename
    ... on Meeting {
      id
      leaderId
      members{
        id
        name
        reaction
      }
      memo
    }
    ... on Member {
      id
      name
      reaction
    }
  }
}
