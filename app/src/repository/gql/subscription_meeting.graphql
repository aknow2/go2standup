subscription SubscribeMeeting($id: String!) {
  meeting (id: $id) {
    ... on Meeting {
      __typename
      id
      leaderId
      members{
        id
        name
        reaction
      }
      memo
    }
    ... on Member {
      __typename
      id
      name
      reaction
    }
  }
}
